services:
  - type: web
    name: my-file-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 -m src"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.16
